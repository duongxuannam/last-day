/**
 * this config use for full screen layer like call, gps enforcer popup
 */
export const LayerIndexValue = {
  // highest because it no block view
  TOAST_LAYER: 20000,
  // highest because it no block view
  SERVER_CONFIG_INFO_LAYER: 20000,
  // call layer required to highest responsible layer
  CALL_LAYER: 10000,
  VERSION_MANAGER_LAYER: 1001,
  GPS_ENFORCER: 1000,
  OTP_LOGIN: 1000,
};
